name = "dowdiness/css-example"

version = "0.1.0"

import {
  "dowdiness/loom@0.1.0",
  "dowdiness/seam@0.1.0",
  "dowdiness/incr@0.14.0",
}

readme = "README.md"

repository = "https://github.com/dowdiness/loom"

license = "Apache-2.0"

keywords = [ "css", "parser", "example" ]

description = "CSS parser example exercising #loom.rule ~ (Emit), ! (EmitOr), @until (ErrorUntil)"

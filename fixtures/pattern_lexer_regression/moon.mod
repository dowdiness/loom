name = "dowdiness/loom_tests/pattern_lexer_regression"

version = "0.1.0"

import {
  "dowdiness/loom@0.1.0",
}

readme = "README.md"

repository = "https://github.com/dowdiness/loom"

license = "Apache-2.0"

description = "Compile and runtime regression fixture for generated #loom.pattern lexers"

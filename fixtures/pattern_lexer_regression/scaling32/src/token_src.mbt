///|
#loom.token
pub(all) enum Token {
  #loom.literal
  #loom.pattern("(!)")
  #loom.payload("record_payload($0.to_owned())")
  PayloadShort(String)
  #loom.literal
  #loom.pattern("(!!)")
  #loom.payload("record_payload($0.to_owned())")
  PayloadLong(String)
  #loom.literal
  #loom.pattern("a[0-9]+")
  CandidateA
  #loom.literal
  #loom.pattern("b[0-9]+")
  CandidateB
  #loom.literal
  #loom.pattern("c[0-9]+")
  CandidateC
  #loom.literal
  #loom.pattern("d[0-9]+")
  CandidateD
  #loom.literal
  #loom.pattern("e[0-9]+")
  CandidateE
  #loom.literal
  #loom.pattern("f[0-9]+")
  CandidateF
  #loom.literal
  #loom.pattern("g[0-9]+")
  CandidateG
  #loom.literal
  #loom.pattern("h[0-9]+")
  CandidateH
  #loom.literal
  #loom.pattern("i[0-9]+")
  CandidateI
  #loom.literal
  #loom.pattern("j[0-9]+")
  CandidateJ
  #loom.literal
  #loom.pattern("k[0-9]+")
  CandidateK
  #loom.literal
  #loom.pattern("l[0-9]+")
  CandidateL
  #loom.literal
  #loom.pattern("m[0-9]+")
  CandidateM
  #loom.literal
  #loom.pattern("n[0-9]+")
  CandidateN
  #loom.literal
  #loom.pattern("o[0-9]+")
  CandidateO
  #loom.literal
  #loom.pattern("p[0-9]+")
  CandidateP
  #loom.literal
  #loom.pattern("q[0-9]+")
  CandidateQ
  #loom.literal
  #loom.pattern("r[0-9]+")
  CandidateR
  #loom.literal
  #loom.pattern("s[0-9]+")
  CandidateS
  #loom.literal
  #loom.pattern("t[0-9]+")
  CandidateT
  #loom.literal
  #loom.pattern("u[0-9]+")
  CandidateU
  #loom.literal
  #loom.pattern("v[0-9]+")
  CandidateV
  #loom.literal
  #loom.pattern("w[0-9]+")
  CandidateW
  #loom.literal
  #loom.pattern("x[0-9]+")
  CandidateX
  #loom.literal
  #loom.pattern("y[0-9]+")
  CandidateY
  #loom.literal
  #loom.pattern("z[0-9]+")
  CandidateZ
  #loom.literal
  #loom.pattern("A[0-9]+")
  CandidateAA
  #loom.literal
  #loom.pattern("B[0-9]+")
  CandidateAB
  #loom.literal
  #loom.pattern("C[0-9]+")
  CandidateAC
  #loom.literal
  #loom.pattern("D[0-9]+")
  CandidateAD
  #loom.literal
  #loom.pattern("E[0-9]+")
  CandidateAE
  #loom.literal
  #loom.pattern("F[0-9]+")
  CandidateAF
  #loom.eof
  EOF
} derive(Eq, Debug)

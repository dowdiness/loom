// Generated by loomgen (#521) — do not edit.

///|
/// Step-based lexer (longest-match; declaration order breaks ties).
pub fn step_lex_legacy(source : String, pos : Int) -> @core.LexStep[Token] {
  let cursor = @core.LexCursor::new(source, pos)
  if cursor.is_done() {
    return Done
  }
  let view = cursor.view()
  let mut best_len = 0
  let mut best : Token? = None
  match re"^(?:(!))".execute(view) {
    Some(m) => {
      let len = m.content().length()
      if len > best_len {
        best_len = len
        best = Some(PayloadShort(record_payload(m.content().to_owned())))
      }
    }
    None => ()
  }
  match re"^(?:(!!))".execute(view) {
    Some(m) => {
      let len = m.content().length()
      if len > best_len {
        best_len = len
        best = Some(PayloadLong(record_payload(m.content().to_owned())))
      }
    }
    None => ()
  }
  if view =~ (re"^(?:a[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateA)
    }
  }
  if view =~ (re"^(?:b[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateB)
    }
  }
  if view =~ (re"^(?:c[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateC)
    }
  }
  if view =~ (re"^(?:d[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateD)
    }
  }
  if view =~ (re"^(?:e[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateE)
    }
  }
  if view =~ (re"^(?:f[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateF)
    }
  }
  if view =~ (re"^(?:g[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateG)
    }
  }
  if view =~ (re"^(?:h[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateH)
    }
  }
  if view =~ (re"^(?:i[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateI)
    }
  }
  if view =~ (re"^(?:j[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateJ)
    }
  }
  if view =~ (re"^(?:k[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateK)
    }
  }
  if view =~ (re"^(?:l[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateL)
    }
  }
  if view =~ (re"^(?:m[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateM)
    }
  }
  if view =~ (re"^(?:n[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateN)
    }
  }
  if view =~ (re"^(?:o[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateO)
    }
  }
  if view =~ (re"^(?:p[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateP)
    }
  }
  if view =~ (re"^(?:q[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateQ)
    }
  }
  if view =~ (re"^(?:r[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateR)
    }
  }
  if view =~ (re"^(?:s[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateS)
    }
  }
  if view =~ (re"^(?:t[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateT)
    }
  }
  if view =~ (re"^(?:u[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateU)
    }
  }
  if view =~ (re"^(?:v[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateV)
    }
  }
  if view =~ (re"^(?:w[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateW)
    }
  }
  if view =~ (re"^(?:x[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateX)
    }
  }
  if view =~ (re"^(?:y[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateY)
    }
  }
  if view =~ (re"^(?:z[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateZ)
    }
  }
  if view =~ (re"^(?:A[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAA)
    }
  }
  if view =~ (re"^(?:B[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAB)
    }
  }
  if view =~ (re"^(?:C[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAC)
    }
  }
  if view =~ (re"^(?:D[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAD)
    }
  }
  if view =~ (re"^(?:E[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAE)
    }
  }
  if view =~ (re"^(?:F[0-9]+)", after=rest) {
    let len = view.length() - rest.length()
    if len > best_len {
      best_len = len
      best = Some(CandidateAF)
    }
  }
  match best {
    None =>
      @core.LexStep::Invalid(
        at=pos,
        width=@core.next_char_offset(source, pos) - pos,
        message="Unexpected character",
      )
    Some(tok) => {
      cursor.advance_code_units(best_len)
      cursor.produced(tok)
    }
  }
}

// Generated by loomgen (#521) — do not edit.

///|
/// Step-based lexer (longest-match; declaration order breaks ties).
pub fn step_lex_first(source : String, pos : Int) -> @core.LexStep[Token] {
  let cursor = @core.LexCursor::new(source, pos)
  if cursor.is_done() {
    return Done
  }
  let view = cursor.view()
  let mut best_len = 0
  let mut best : Token? = None
  if source[pos] == 33 {
    match re"^(?:(!))".execute(view) {
      Some(m) => {
        let len = m.content().length()
        if len > best_len {
          best_len = len
          best = Some(PayloadShort(record_payload(m.content().to_owned())))
        }
      }
      None => ()
    }
    match re"^(?:(!!))".execute(view) {
      Some(m) => {
        let len = m.content().length()
        if len > best_len {
          best_len = len
          best = Some(PayloadLong(record_payload(m.content().to_owned())))
        }
      }
      None => ()
    }
  } else if source[pos] == 97 {
    if view =~ (re"^(?:a[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateA)
      }
    }
  } else if source[pos] == 98 {
    if view =~ (re"^(?:b[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateB)
      }
    }
  } else if source[pos] == 99 {
    if view =~ (re"^(?:c[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateC)
      }
    }
  } else if source[pos] == 100 {
    if view =~ (re"^(?:d[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateD)
      }
    }
  } else if source[pos] == 101 {
    if view =~ (re"^(?:e[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateE)
      }
    }
  } else if source[pos] == 102 {
    if view =~ (re"^(?:f[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateF)
      }
    }
  } else if source[pos] == 103 {
    if view =~ (re"^(?:g[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateG)
      }
    }
  } else if source[pos] == 104 {
    if view =~ (re"^(?:h[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateH)
      }
    }
  } else if source[pos] == 105 {
    if view =~ (re"^(?:i[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateI)
      }
    }
  } else if source[pos] == 106 {
    if view =~ (re"^(?:j[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateJ)
      }
    }
  } else if source[pos] == 107 {
    if view =~ (re"^(?:k[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateK)
      }
    }
  } else if source[pos] == 108 {
    if view =~ (re"^(?:l[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateL)
      }
    }
  } else if source[pos] == 109 {
    if view =~ (re"^(?:m[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateM)
      }
    }
  } else if source[pos] == 110 {
    if view =~ (re"^(?:n[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateN)
      }
    }
  } else if source[pos] == 111 {
    if view =~ (re"^(?:o[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateO)
      }
    }
  } else if source[pos] == 112 {
    if view =~ (re"^(?:p[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateP)
      }
    }
  } else if source[pos] == 113 {
    if view =~ (re"^(?:q[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateQ)
      }
    }
  } else if source[pos] == 114 {
    if view =~ (re"^(?:r[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateR)
      }
    }
  } else if source[pos] == 115 {
    if view =~ (re"^(?:s[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateS)
      }
    }
  } else if source[pos] == 116 {
    if view =~ (re"^(?:t[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateT)
      }
    }
  } else if source[pos] == 117 {
    if view =~ (re"^(?:u[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateU)
      }
    }
  } else if source[pos] == 118 {
    if view =~ (re"^(?:v[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateV)
      }
    }
  } else if source[pos] == 119 {
    if view =~ (re"^(?:w[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateW)
      }
    }
  } else if source[pos] == 120 {
    if view =~ (re"^(?:x[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateX)
      }
    }
  } else if source[pos] == 121 {
    if view =~ (re"^(?:y[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateY)
      }
    }
  } else if source[pos] == 122 {
    if view =~ (re"^(?:z[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateZ)
      }
    }
  } else if source[pos] == 65 {
    if view =~ (re"^(?:A[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAA)
      }
    }
  } else if source[pos] == 66 {
    if view =~ (re"^(?:B[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAB)
      }
    }
  } else if source[pos] == 67 {
    if view =~ (re"^(?:C[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAC)
      }
    }
  } else if source[pos] == 68 {
    if view =~ (re"^(?:D[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAD)
      }
    }
  } else if source[pos] == 69 {
    if view =~ (re"^(?:E[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAE)
      }
    }
  } else if source[pos] == 70 {
    if view =~ (re"^(?:F[0-9]+)", after=rest) {
      let len = view.length() - rest.length()
      if len > best_len {
        best_len = len
        best = Some(CandidateAF)
      }
    }
  }
  match best {
    None =>
      @core.LexStep::Invalid(
        at=pos,
        width=@core.next_char_offset(source, pos) - pos,
        message="Unexpected character",
      )
    Some(tok) => {
      cursor.advance_code_units(best_len)
      cursor.produced(tok)
    }
  }
}

name = "dowdiness/loom_tests/grammar_ir_regression"

version = "0.1.0"

import {
  "dowdiness/loom@0.1.0",
  "dowdiness/seam@0.1.0",
}

readme = "README.md"

repository = "https://github.com/dowdiness/loom"

license = "Apache-2.0"

description = "Compile-regression fixture for loomgen --grammar-ir output"
